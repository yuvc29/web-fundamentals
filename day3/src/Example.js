import React from 'react'
import ReactDOM from 'react-dom'

const Example = (props) => {
    return (
        <h1>
            {`Hello ${props.name}, Welcome to Fareye`}
        </h1>
    )
}

export default Example;